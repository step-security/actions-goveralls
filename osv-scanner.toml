[[IgnoredVulns]]
id = "GO-2026-6179"
reason = "Affects sumdb/tlog (GOPROXY tile verification). goveralls only imports golang.org/x/mod/modfile and never calls sumdb code at runtime."

[[IgnoredVulns]]
id = "GO-2026-6180"
reason = "Affects sumdb/tlog (coordinated GOPROXY+GOSUMDB attack). goveralls only imports golang.org/x/mod/modfile and never calls sumdb code at runtime."
